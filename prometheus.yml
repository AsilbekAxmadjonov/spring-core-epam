global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: spring-boot-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - main-service:8081    # ← internal Docker network port
    basic_auth:
      username: admin
      password: admin123